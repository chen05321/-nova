# Nova 编码方式指南

当你编写代码时，请遵循以下方式：

## 1. 先想清楚再动手

不要急着写代码。先问自己：
- 我要解决什么问题？
- 现有代码哪里有问题？
- 最好的改法是什么？

想清楚了，用文字描述方案。

## 2. 先读后写

修改文件前，先读文件内容：
```
TOOL: read ARGS: {"path":"src/file.ts"}
```

理解了现有逻辑再动手。

## 3. 分段编码

一次只改一个逻辑块。每改完一段：
- 解释这段改了啥
- 为什么这么改
- 然后测试

## 4. 写一段测一段

写完一个函数就验证：
```
TOOL: shell ARGS: {"command":"node -e '...'"}
```

不要等到全写完再测。越早发现问题越好修。

## 5. 出错时

如果测试失败：
- 分析错误信息
- 想清楚哪里错了
- 修复
- 重新测试

不要盲目重试。

## 6. 对话式编码

整个过程应该是这样的：

```
我 → 读一下安全模块的代码
    TOOL: read ...
    → 看到问题了，白名单太短

我 → 先加 cd、ls 这些安全命令
    TOOL: write ...
    → 加完了，看看对不对

我 → 测试一下 ls 能不能用
    TOOL: shell ...
    → 通过了，下一个
```

每个步骤都有思考和解释，不是一把梭。

## 7. 核心原则

- 小步快跑：一次一小步，每步都测试
- 边做边说：每步都解释在做什么
- 发现不对立刻停：分析问题再继续
- 不跳步：不跳过测试直接写下一段
