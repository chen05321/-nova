# Nova 扩展接入文档

## 飞书 / 企业微信接入

需要创建一个独立的服务端，部署后 Nova 作为后端 AI 引擎。

### 架构

```
用户 → 飞书/企微 → Webhook → Nova Server → Nova Agent
                                           → 响应返回
```

### 飞书接入（30 分钟）

```bash
# 1. 创建飞书应用
#    https://open.feishu.cn/app

# 2. 启动 Nova Webhook 服务
nova --serve

# 3. 在飞书应用配置事件订阅 URL
#    http://你的服务器:3901/webhook/feishu
```

### 企业微信接入

```bash
# 1. 创建企业微信应用
# 2. 配置回调 URL 指向 Nova
# 3. 启动时加参数
nova --serve --wecom
```

---

## 多 Agent 架构

多 Agent 需要进程级隔离。支持两种模式：

### 模式一：单进程多 Agent（轻量）

```typescript
const agent1 = new NovaAgent();
const agent2 = new NovaAgent();
// 各自独立运行，共享事件总线
```

### 模式二：多进程 Agent（生产）

每个 Agent 独立进程，通过 Redis/RabbitMQ 通信：

```
Agent A (编程) ─┐
Agent B (设计) ─┼→ 消息队列 → 路由分发
Agent C (写作) ─┘
```

Nova 内置的 `/role` 系统支持同一进程内的角色切换。多进程方案需要部署编排工具（Docker/K8s）。

---

## 时序说明

以上功能（飞书、企微、多 Agent 集群）属于**生产级部署**范畴，当前内测版本通过以下方式覆盖：

| 需求 | 当前支持 | 生产方案 |
|:----|:--------|:---------|
| 多角色 | ✅ `/role` 命令 | 多进程 + 独立配置 |
| 模型切换 | ✅ `/model` 命令 | 自动路由 |
| 任务管理 | ✅ `/todo` `/tasks` `/done` | 持久化 + 定时触发 |
| 飞书接入 | ❌ 需搭建服务 | `nova --serve` |
| 企业微信接入 | ❌ 需搭建服务 | `nova --serve --wecom` |
| 多 Agent | ❌ 需进程管理 | Docker Compose |
